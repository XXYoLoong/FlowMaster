####################################
# Flask 基础配置
####################################
# Flask 会话密钥（生产环境必须修改）
SECRET_KEY=your-flask-secret-key-here

# 是否开启调试模式（生产必须为 False）
FLASK_DEBUG=False

# Web 服务监听地址与端口
HOST=0.0.0.0
PORT=5000


####################################
# JWT 配置
####################################
# JWT 签名密钥（生产环境必须修改）
JWT_SECRET_KEY=your-jwt-secret-key-here


####################################
# 数据加密配置
####################################
# 对称加密密钥（32 字节，Base64 / 随机字符串）
ENCRYPTION_KEY=your-32-byte-encryption-key-here


####################################
# 数据库配置（MySQL）
####################################

# ✅ 场景一：生产环境 / 已连接 VPN（推荐）
# 使用数据库服务器 VPN 内网 IP
DATABASE_URL=mysql+pymysql://db_user:db_password@db_vpn_ip:3306/db_name

# 示例：
# DATABASE_URL=mysql+pymysql://flowmaster_user:password@10.0.0.1:3306/flowmaster


# ✅ 场景二：本地开发（通过 SSH 隧道）
# 当未连接 VPN，但通过 ssh -L 转发端口时使用
# DATABASE_URL=mysql+pymysql://db_user:db_password@127.0.0.1:13306/db_name


####################################
# DeepSeek API 配置（可选）
####################################
DEEPSEEK_API_KEY=your-deepseek-api-key
DEEPSEEK_API_BASE=https://api.deepseek.com


####################################
# 通义千问 API 配置（可选）
####################################
# 二选一即可（推荐 DASHSCOPE_API_KEY）

# DASHSCOPE_API_KEY=your-dashscope-api-key
QIANWEN_API_KEY=your-qianwen-api-key

QIANWEN_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
